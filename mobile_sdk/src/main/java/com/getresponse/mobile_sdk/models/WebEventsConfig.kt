package com.getresponse.mobile_sdk.models

data class WebEventsConfig(    val endpoint: String,
                               val auth: AuthConfig,
                               val options: EndpointOptions)
